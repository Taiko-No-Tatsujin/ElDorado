import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';


const HomePage = () => (
  <Card className="container">
    <CardTitle title="EL-Dorado" subtitle="The game of online." />
  </Card>
);

export default HomePage;